name: sumo_bracket_server
version: 0.1.0

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  shelf: ^1.4.2
  shelf_router: ^1.0.0
  mysql1: ^0.19.0
  json_annotation: ^4.8.0
  logging: ^1.1.1

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.6.0
